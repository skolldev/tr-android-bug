import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tr-android-bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
